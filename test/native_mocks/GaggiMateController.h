// Stub for GaggiMateController.h — only needed because DimmedPump.cpp has
// a gratuitous `#include <GaggiMateController.h>` at line 3 that doesn't
// actually reference any symbol from it (verified via grep). Empty stub
// satisfies the parse without dragging in BLE / config / every peripheral.

#pragma once
#ifndef NATIVE_MOCKS_GAGGIMATECONTROLLER_H
#define NATIVE_MOCKS_GAGGIMATECONTROLLER_H

#endif // NATIVE_MOCKS_GAGGIMATECONTROLLER_H
